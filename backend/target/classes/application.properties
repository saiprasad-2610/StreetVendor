spring.application.name=svms
server.port=8080

spring.datasource.url=jdbc:mysql://localhost:3306/svms_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=26102004
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

server.error.include-message=always
server.error.include-binding-errors=always

jwt.secret=SSVMS2024SolapurMunicipalCorporationSecretKeyForJWTTokenGenerationMustBeLongEnough256Bits!!
jwt.expiration=86400000
jwt.refresh-expiration=604800000

logging.level.org.springframework.security=DEBUG
logging.level.com.smc.svms=DEBUG

spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

app.base-url=http://localhost:8080
app.qr-code-dir=./qr-codes
app.upload-dir=./uploads/violations
app.location.threshold-meters=4.0

# Razorpay Config (Test Keys)
razorpay.key.id=rzp_test_SiOwSNyPMiHeZN
razorpay.key.secret=6ePNLiA1wZMz63xixH0ssmYF
razorpay.currency=INR

# Redis Configuration (Disabled for now)
# spring.redis.host=localhost
# spring.redis.port=6379
# spring.redis.timeout=2000ms
# spring.redis.jedis.pool.max-active=8
# spring.redis.jedis.pool.max-idle=8
# spring.redis.jedis.pool.min-idle=0

# Spring Cache Configuration (Disabled for now)
# spring.cache.type=redis
# spring.cache.redis.time-to-live=600000

# Mail Configuration
# IMPORTANT: Update these with your actual email credentials
# For Gmail: Use App Password (not your regular password)
# Generate at: https://myaccount.google.com/apppasswords
spring.mail.host=smtp.gmail.com
spring.mail.port=465
spring.mail.username=saiprasad26102004@gmail.com
spring.mail.password=rdvmtrsnvvsnkyxm

spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.ssl.enable=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# Enhanced Security Configuration
app.jwt.secret=SSVMS2024SolapurMunicipalCorporationSecretKeyForJWTTokenGenerationMustBeLongEnough256Bits!!
app.jwt.expiration=86400000
app.jwt.refresh-expiration=604800000
app.encryption.key=SMC2024EncryptionKeyForAES256BitEncryptionMustBe32Characters!!

# Rate Limiting Configuration
app.rate-limit.login.max-attempts=5
app.rate-limit.login.block-duration-minutes=15
app.rate-limit.api.requests-per-minute=100
app.rate-limit.scan.requests-per-minute=50

# File Upload Configuration
app.upload-dir=uploads/violations
app.qr-code-dir=qr-codes
app.max-file-size=10MB

# Geofencing Configuration
app.location.threshold-meters=4.0
app.geofencing.polygon-precision=6
app.geofencing.max-distance-meters=1000
app.expected-city=Solapur

# Advanced Geofencing Configuration
geofencing.use-advanced-algorithms=true
geofencing.default-gps-accuracy=10.0
geofencing.confidence-threshold=0.7
geofencing.adaptive-thresholds.enabled=true

# Google Geolocation API Configuration
app.google.geolocation.api-key=AIzaSyDoa5Dau1p7AZdAB0rH1Z_n0l_rhVYMhpU
app.google.geolocation.enabled=true

# Monitoring Configuration
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=when-authorized

# Logging Configuration
logging.level.org.springframework.security=DEBUG
logging.level.com.smc.svms=INFO
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} - %msg%n
